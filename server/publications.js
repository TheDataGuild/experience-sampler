/* We don't need this if we have autopublish turned on */

/* Publish collections to clients here */

/* For example, you can enable this once you uninstall autopublish */
/*
Meteor.publish('users', function() {
  return Meteor.users.find({});
});
*/

